mongoDBStart.cfg is the config file for running mongoDB as a single instance service
repSetDb0.cfg, repSetDb1.cfg and repSetDb2.cfg are the config files used to install a replica set. 
These files need to be placed in the bin folder  holding the mongo.exe file, typically, this is at C:\mongodb\bin.